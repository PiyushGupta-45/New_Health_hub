name: health_hub
description: "A new Flutter project."

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ^3.8.0

dependencies:
  flutter:
    sdk: flutter
  health: ^13.2.0 # Add the health_connect_plus dependency
  intl: ^0.20.2
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  shared_preferences: ^2.2.3
  flutter_lints: ^5.0.0
  flutter_launcher_icons: ^0.14.1 

flutter_icons:
  android: true
  ios: true
  image_path: "assets/main_logo.png"

flutter:

  uses-material-design: true

  assets:
    - assets/main_logo.png
